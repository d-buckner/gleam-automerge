[package]
name = "automerge_nif"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustler = "0.37"
automerge = "0.5"
serde_json = "1"

[features]
default = ["rustler/default"]
