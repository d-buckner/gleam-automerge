/_build/
/deps/
# Synthetic mix.exs files for Gleam-only packages — these are generated by
# mix gleam.deps.get and must be committed so consumers don't need to run setup
!deps/gleam_stdlib/mix.exs
!deps/gleeunit/mix.exs
/priv/native/
native/*/target/
*.ez
erl_crash.dump

# Rust binary artifacts
/target/
